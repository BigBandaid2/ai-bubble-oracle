# Rebuilt from Yahoo + the committed H100 CSV on every run, so not tracked.
oracle.db

__pycache__/
*.pyc

# Vercel CLI link metadata
.vercel/

.vercel

# Local Claude Code preview/launch config (dev-only)
.claude/
